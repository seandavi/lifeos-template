---
type: dashboard
---

# ✅ Action Dashboard

> **Requires the Tasks community plugin.** If the blocks below render as raw ```tasks code, install/enable Tasks (see [`docs/obsidian-setup.md`](../docs/obsidian-setup.md)) and reload. Until then this file degrades to readable code blocks — nothing breaks.
>
> Add `📅 YYYY-MM-DD` to any `- [ ]` line to give it a due date; the date views below fill in as you do. Grouping by file/heading works with plain checkboxes today.

## 📥 Open — inbox, grouped by section
```tasks
not done
path includes inbox
group by heading
short mode
```

## 📓 Open — recent journals
```tasks
not done
path includes journal
group by filename
short mode
```

## ⏰ Anything with a due date (soonest first)
```tasks
not done
has due date
sort by due
```

## 🔴 Overdue
```tasks
not done
due before today
sort by due
```
